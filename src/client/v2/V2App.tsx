/**
 * V2 UI entry stub.
 * Future UI work should start by reading `src/client/api.ts`, which documents
 * the browser-to-runtime contract this tree is expected to use.
 */
export const V2App = () => (
  <main className="app-shell">
    <section className="workspace" aria-label="V2 UI stub">
      <h1>V2 UI Stub</h1>
      <p>This route is reserved for the next React UI.</p>
      <p>Start with <code>src/client/api.ts</code> before building against the local runtime.</p>
    </section>
  </main>
);
