import { describe, expect, it } from 'vitest';

describe('V2 API router', () => {
    it('lists sessions and coerces includePartyContext to boolean', () => {
        expect.fail('Not implemented.');
    });

    it('filters sessions by mode', () => {
        expect.fail('Not implemented.');
    });

    it('returns a session feed grouped by run with ordered session entries', () => {
        expect.fail('Not implemented.');
    });

    it('returns not found for feed requests without a session id', () => {
        expect.fail('Not implemented.');
    });

    it('starts a run from POST /api/v2/runs', () => {
        expect.fail('Not implemented.');
    });

    it('reads refresh state from GET /api/v2/refresh', () => {
        expect.fail('Not implemented.');
    });

    it('starts refresh from POST /api/v2/refresh', () => {
        expect.fail('Not implemented.');
    });

    it('lists NPCs with default pagination', () => {
        expect.fail('Not implemented.');
    });

    it('lists NPCs with skip take and filter query params', () => {
        expect.fail('Not implemented.');
    });

    it('reads additional context as markdown', () => {
        expect.fail('Not implemented.');
    });

    it('writes additional context as markdown', () => {
        expect.fail('Not implemented.');
    });

    it('streams console SSE entries', () => {
        expect.fail('Not implemented.');
    });

    it('streams runtime SSE events', () => {
        expect.fail('Not implemented.');
    });

    it('maps tagged application errors to structured API error responses', () => {
        expect.fail('Not implemented.');
    });

    it('returns 404 for unknown routes', () => {
        expect.fail('Not implemented.');
    });
});
