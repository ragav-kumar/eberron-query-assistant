import { describe, expect, it } from 'vitest';

describe('v2 corpus boundary', () => {
    it('builds foundry source changes from export records', () => {
        expect.fail('Not implemented.');
    });

    it('builds pdf source changes from parsed pages', () => {
        expect.fail('Not implemented.');
    });

    it('builds article source changes from fetched article content', () => {
        expect.fail('Not implemented.');
    });

    it('chunks normalized text into stable chunk boundaries', () => {
        expect.fail('Not implemented.');
    });

    it('refreshes retrieval embeddings and reuses compatible vectors', () => {
        expect.fail('Not implemented.');
    });

    it('deletes the legacy vector-index sidecar during force rebuild', () => {
        expect.fail('Not implemented.');
    });

    it('returns lexical results for plain-text search', () => {
        expect.fail('Not implemented.');
    });

    it('merges lexical and semantic matches into hybrid-ranked results', () => {
        expect.fail('Not implemented.');
    });

    it('applies sourceType and sourceKey filters during search', () => {
        expect.fail('Not implemented.');
    });

    it('returns no results for empty queries', () => {
        expect.fail('Not implemented.');
    });

    it('disables in-memory vector caching for oversized corpus databases', () => {
        expect.fail('Not implemented.');
    });

    it('builds party context from configured actors session notes and quest journals', () => {
        expect.fail('Not implemented.');
    });

    it('returns explanatory party-context fallbacks when actor UUIDs are missing', () => {
        expect.fail('Not implemented.');
    });

    it('returns explanatory party-context fallbacks when corpus.sqlite is absent', () => {
        expect.fail('Not implemented.');
    });
});
